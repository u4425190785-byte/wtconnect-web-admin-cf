# Cloudflare Pages — wtconnect-web-admin-cf (Super Admin Byte)
# Projet Pages SÉPARÉ du portail user (wt-connect-web-cf).
name = "wtconnect-web-admin-cf"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".vercel/output/static"
